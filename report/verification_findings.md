# Verification findings (adversarial multi-agent review)

Workflow run wf_e95bac47-58d · 18 confirmed of 23 raw findings (7 dimensions, each finding adversarially re-checked).

## Confirmed (sorted by severity)

### 1. [CRITICAL] In-dist vs OOD foresight gap is confounded by opposite-signed return autocorrelation across the two windows (regime confound)
- **dimension/file:** design-confounds · `config.py` IN_DIST (lines 48-53) and OOD (lines 57-62); metric in src/leakage/metrics/leakage.py foresight_gap (lines 157-160) and next_day_prescience exposure_timing (lines 59-67)
- **claim:** The headline 'clean' estimate of leakage is the within-model T-in (2024-H2) minus C-B (2026) prescience gap, justified as holding the model fixed so only 'has it seen this era' changes. But the two windows have materially different short-horizon dynamics. I measured equal-weight market 1-day autocorrelation = +0.004 in 2024-H2 vs -0.085 in 2026, and the windows differ in trend/vol (2024-H2: +23.2% total, ann vol 0.323, Sharpe 1.44; 2026: +5.5%, ann vol 0.251, Sharpe 0.66). Since T-in and C-B are the identical model with the identical prompt and behave as a trailing-momentum/exposure trader, ex
- **fix:** Add a no-foresight benchmark run on BOTH windows (the existing MockClient momentum trader, or a fixed buy-and-hold / trailing-momentum rule) and report the foresight gap of T-in/C-B RELATIVE TO that benchmark's own in-dist-minus-OOD gap; i.e. difference-in-differences against a memory-free agent. Alternatively, neutralize the regime by also matching windows on realized autocorrelation/vol, or by e

### 2. [HIGH] event_day_dodge compares portfolio return ON the event day against the market return realized the day AFTER the event (off-by-one in the market series)
- **dimension/file:** metric-validity · `leakage.py` event_day_dodge, lines 97-115 (esp. line 105 default `mkt` and line 112 `mkt.loc[d]`); compounded by orchestrate.py line 74 passing `_market_next(r)`
- **claim:** `pr = result.port_returns` is indexed by REALIZED date, so `pr.loc[d]` for event day d is the portfolio return realized ON day d = sum_i w_i(d-1)*r_i(d-1->d) — correct. But the market benchmark `mkt = result.next_day_returns.mean(axis=1)` (also via orchestrate `_market_next`) is indexed by DECISION date: `next_day_returns.loc[d]` = rets.loc[d+1] = the market move from d to d+1. So `mkt.loc[d]` is the market return realized the day AFTER the event, while `port_r` is the return realized ON the event day. The two sides of `excess_vs_mkt = port_r - mkt_r` are one trading day apart.
- **fix:** Benchmark the market on the SAME realized date as port_returns. Either (a) build a realized-date-indexed market series: `mkt_realized = result.next_day_returns.mean(axis=1)`; then shift its index forward one trading day to the realized date before lookup, or simpler (b) reindex by using the prior decision day: for event day d find the last decision day < d and use `result.next_day_returns.loc[prev

### 3. [HIGH] Pooling per-day contributions across seeds is pseudo-replication: inflates n and understates variance/p-values in the headline tests
- **dimension/file:** stats-rigor · `orchestrate.py` _pooled_timing_contrib (lines 49-56); consumed in evaluate() lines 96-105 by stats.block_bootstrap_mean_ci and stats.permutation_diff
- **claim:** _pooled_timing_contrib concatenates the per-day exposure-timing contributions from all 3 seeds end-to-end (pd.concat(..., ignore_index=True)). The pooled series is then passed to block_bootstrap_mean_ci (CI + p_gt_0) and permutation_diff, both of which treat each element as an independent observation. But within a group the 3 seed-runs are over the SAME window/SAME trading days and SAME next-day returns (engine.py: only the LLM sampling seed seed+i differs; the z(target) factor in prescience_contrib is identical across seeds). The contributions are therefore strongly positively correlated acro
- **fix:** Do not pool day×seed as independent units. Pick one of: (1) average the per-day contribution across seeds first (align seeds on decision_date, take the mean across seeds) to get ONE ~127-length series per group, then bootstrap/permute that; this respects the true number of independent time units. (2) Treat the seed-mean per-day series as the unit and additionally report between-seed variability se

### 4. [HIGH] Pre-event timing and event-day-dodge metrics have no OOD counterpart, so they cannot separate leakage from a single lucky regime call
- **dimension/file:** design-confounds · `config.py` EVENT_ANCHORS (lines 87-90) are both 2024 dates; consumed in src/leakage/run/orchestrate.py evaluate (lines 71-76) and src/leakage/metrics/leakage.py pre_event_timing (71-94) / event_day_dodge (97-115)
- **claim:** Both event anchors are in 2024-H2, so pre_event_timing and event_day_dodge only ever produce scores for T-in and C-A (in-dist groups); C-B (the within-model OOD control) has no events in its window and is scored on nothing. There is therefore NO same-model control for these two 'smoking-gun' metrics — the within-model design that the report relies on to neutralize capability (3.2) does not apply to them. A positive T-in pre-event score is then a single realization (2 events) with no null distribution and no OOD baseline; it is indistinguishable from one well-timed regime/momentum call. The rep
- **fix:** Either add matched 'placebo' events in the 2026 OOD window (sharp up/down days) to give C-B comparable scores, or compute a null distribution for pre-event timing via random pseudo-event dates within each window. Report C-A pre-event comparisons with an explicit capability/size-confound caveat, not as primary evidence.

### 5. [HIGH] Parse-fail days inject all-cash (0-exposure) points into leakage metrics, differentially attenuating the treatment group's signal
- **dimension/file:** agent-parsing · `agent.py` agent.py:40,45,89 (_coerce_decision / decide return Decision with target_weights={}, exposure=0, confidence=0); engine.py:108-114 adds these days to weights/exposure/confidence unconditionally; metrics never filter on parse_ok (leakage.py:46-68 next_day_prescience, stats.py:19-24 prescience_contrib via orchestrate.py:54)
- **claim:** When the LLM output cannot be parsed (or the client errors), the agent returns a Decision with empty target_weights -> exposure() == 0 and confidence == 0. In run_backtest these failed days are still written into weights_rows, exposure, and confidence for that date (engine.py:111-116) and into port_returns (port_r becomes 0). parse_ok is used ONLY to increment n_parse_fail (engine.py:108-109); it never excludes the day from any metric input. The headline timing metric exposure_timing = corr(exposure, mkt_next) (leakage.py:58-60) and the bootstrap/permutation headline test prescience_contrib(r.
- **fix:** Exclude parse_ok=False days from ALL metric inputs rather than coercing them to all-cash. Concretely: in run_backtest, skip appending failed-parse days to weights_rows/exposure/confidence/port_returns (or carry the previous day's weights forward as an explicit 'hold' rather than forcing 0), and align the realized-return bookkeeping accordingly. At minimum, in leakage.next_day_prescience and orches

### 6. [HIGH] Model auto-selection ignores the OOD-denial gate the design requires, so an invalid treatment model can be selected and silently reused as the C-B time control
- **dimension/file:** infra-repro · `model_selection.py` pick_best() lines 90-95; docstring lines 1-8; OOD_DENY lines 35-37 / probe lines 76-78
- **claim:** The module docstring states the valid treatment is 'The highest-scoring model that STILL DENIES knowledge of the 2026 OOD window', and probe() asks the OOD_DENY question and stores q1_2026_answer. But pick_best() selects purely by max(score_2024H2) and never inspects the OOD answer (grep confirms q1_2026 is referenced nowhere outside model_selection.py and cutoff_probe.py, and the recorded answer never gates anything). The selected treatment tag is then exported as LEAKAGE_TREATMENT_MODEL and, via config.GROUPS, is used for BOTH T-in (in-dist) AND C-B (the OOD time-control). If the auto-picked
- **fix:** In pick_best(), restrict candidates to those that PASS the OOD-denial check before maximizing score_2024H2. Add a scored boolean (e.g. denies_2026 = the q1_2026 answer expresses uncertainty / does not name a concrete Q1-2026 event) and select best = max over {m : res[m] denies 2026} by score_2024H2; abort with a non-zero exit if no candidate both knows 2024-H2 and denies 2026, so the bootstrap's s

### 7. [HIGH] Model selection can never fail loudly: all probe errors are swallowed, so a totally non-functional ollama silently yields an all-zero scoreboard and a tie-break 'winner'
- **dimension/file:** infra-repro · `model_selection.py` _ask() lines 40-52 (returns '[error: ...]' on any exception); _scores lines 55-58; pick_best line 93; bootstrap.sh line 70 (TREAT=$(... --pick ... | tail -1))
- **claim:** _ask() catches every exception and returns a string like '[error: ...]'; _scores() then returns 0 for that answer. So probe()/pick_best() ALWAYS succeed and ALWAYS print a non-empty winner, even if ollama is unreachable or the candidate models were never pulled. With all scores 0, max(models, key=score) returns the first candidate by stable order (qwen3:32b). The bootstrap captures this via TREAT=$(... | tail -1) and proceeds with a treatment model chosen by accident rather than by measured 2024-H2 recall. There is no assertion that the winning score is > 0, and no check that the cutoff_probe 
- **fix:** After probe(), require the winner's score_2024H2 to exceed a threshold (e.g. >= 2/4) AND require the SANITY probe (wc2022 -> Argentina) to pass for that model; if not, sys.exit(1). Distinguish '[error: ...]' answers from genuine wrong answers and refuse to select a model whose probes errored. The non-zero exit will (correctly) abort the bootstrap via set -e and trigger self-terminate.

### 8. [MEDIUM] Block bootstrap is a fixed-length circular block bootstrap mislabeled as 'stationary'; on the pooled series blocks straddle seed seams and fabricate cross-seed autocorrelation
- **dimension/file:** stats-rigor · `stats.py` _block_indices (lines 27-33), block_bootstrap_mean_ci (36-53), stationary_bootstrap_sharpe (76-95)
- **claim:** _block_indices uses a FIXED block length (block=5) with circular wraparound. That is a circular block bootstrap (Politis-Romano stationary bootstrap requires a random geometric block length, which is absent), so the docstring 'Stationary/circular block bootstrap' and the name stationary_bootstrap_sharpe are inaccurate. Coverage itself is correct (verified: returns exactly n in-range indices, proper mod-n wraparound, no off-by-one). The real validity problem appears only because of STAT-1: the bootstrap runs on the seed-concatenated pooled series, so blocks crossing the internal seam (e.g. last
- **fix:** Apply the bootstrap per-seed (or per single seed-mean series) so blocks never cross runs (this is subsumed by the STAT-1 fix of not pooling). Rename and correct the docstring to 'circular fixed-length block bootstrap', or implement a true stationary bootstrap by drawing each block length from Geometric(1/block). If runs must be concatenated, mark seam positions and forbid blocks from spanning them

### 9. [MEDIUM] p_gt_0 uses a percentile-bootstrap p-value (non-pivotal, biased) and no multiple-comparison control across the headline/per-group tests
- **dimension/file:** stats-rigor · `stats.py` block_bootstrap_mean_ci p_gt_0 = (boots <= 0).mean() (line 51); also evaluate() runs two headline comparisons without correction (orchestrate.py 96-105)
- **claim:** p_gt_0 is computed as the fraction of bootstrap means <= 0. A naive percentile-bootstrap p-value is well known to be non-pivotal and biased (it does not invert a properly studentized statistic), so its calibration as a true one-sided p-value is unreliable, especially at the small n here (~100-128 days, fewer effective after STAT-1). Separately, evaluate() performs two headline comparisons (T-in vs C-A and T-in vs C-B) plus numerous per-group prescience/pre-event/dodge metrics, with no Bonferroni/FDR adjustment and no acknowledgement of the small-sample, 3-seed underpowering in the reported tab
- **fix:** Replace the percentile-bootstrap p_gt_0 with either a studentized/BCa bootstrap p-value or simply report the bootstrap CI and let the permutation test carry inference. Apply a multiple-comparison correction (e.g. Holm/Bonferroni over the family of comparisons, or FDR) and report adjusted p-values. Add an explicit power/sample-size caveat (true n ≈ number of decision days, not days×seeds) in the re

### 10. [MEDIUM] Bootstrap CIs and permutation p-values are computed on per-day contributions concatenated across seeds (pseudo-replication)
- **dimension/file:** design-confounds · `orchestrate.py` _pooled_timing_contrib (lines 49-56) concatenates per-day contribs across seeds; consumed by stats.block_bootstrap_mean_ci and stats.permutation_diff (lines 96-105); stats in src/leakage/metrics/stats.py
- **claim:** _pooled_timing_contrib concatenates the per-day exposure-timing contributions from all 3 seeds into one series, then block_bootstrap_mean_ci and permutation_diff treat those rows as exchangeable units. But the same calendar day appears once per seed (3x) and is highly correlated across seeds (same prices, same model, only sampling temperature differs), so the effective sample size is ~n_days, not 3*n_days. This inflates the apparent n and shrinks CIs / p-values. Additionally, the circular block bootstrap in stats._block_indices (lines 27-33) wraps across the seed-concatenation boundary, joinin
- **fix:** Aggregate to one contribution per day (mean across seeds) before bootstrapping/permuting, or use a seed-aware/clustered resampling (resample whole seeds or whole windows, not pooled days). Ensure block bootstrap operates within a single contiguous time series so circular wrap does not cross seed boundaries.

### 11. [MEDIUM] Prepared-data S3 sync is non-fatal (|| true); a missing parquet silently triggers live yfinance on EC2, contradicting the 'no yfinance / reproducible cache' guarantee
- **dimension/file:** infra-repro · `bootstrap.sh` bootstrap.sh line 60 (aws s3 sync data || true); src/leakage/data/ingest.py load_prices lines 61-65 -> download_prices
- **claim:** bootstrap.sh line 60 syncs prepared prices with a trailing '|| true', so a missing/empty data prefix is non-fatal. load_prices() (ingest.py lines 61-65) falls back to download_prices() (live yfinance) whenever the per-window parquet is absent. README.md and ingest.py's header both assert the run uses prepared caches and 'no yfinance on EC2'. If stage.sh wasn't run, the bucket prefix is wrong, or the sync partially fails, the EC2 run will instead pull live data from Yahoo at run time. That data is fetched at the mid-2026 run date with auto_adjust=True, so adjusted historical prices (splits/divs
- **fix:** Make the data presence a hard precondition: after the sync, assert the expected parquets exist (e.g. test -f data/raw/prices_2024H2.parquet && test -f data/raw/prices_2026JanMay.parquet) and exit non-zero (-> self-terminate) if missing. Drop the silent yfinance fallback in load_prices() on EC2 (e.g. raise if the cache is absent and an env flag LEAKAGE_OFFLINE=1 is set), so the experiment never sil

### 12. [MEDIUM] Permutation/bootstrap on timing contributions pools the same trading days across 3 seeds, violating the exchangeability/independence the tests assume
- **dimension/file:** infra-repro · `orchestrate.py` _pooled_timing_contrib lines 49-56; evaluate lines 96-105; stats.permutation_diff / block_bootstrap_mean_ci
- **claim:** _pooled_timing_contrib concatenates per-day prescience_contrib across all seeds for a group, and the result feeds stats.permutation_diff and block_bootstrap_mean_ci. The same calendar trading day appears once per seed (3x), and same-day contributions across seeds are strongly correlated (same realized next-day return, similar exposure). The permutation test shuffles a pooled label vector treating each (seed, day) row as exchangeable, and the block bootstrap's blocks are formed over the concatenated, seed-stacked index — so circular blocks straddle seed boundaries and the effective sample size 
- **fix:** Either aggregate to one contribution series per trading day before testing (e.g. average the per-day contribution across seeds, giving one row per date) and run the bootstrap/permutation on that, or use a cluster/block scheme that respects the (seed, date) structure and does not let bootstrap blocks cross seed boundaries. Note: this is a statistics-validity issue adjacent to my assigned infra dime

### 13. [LOW] Reflection text mislabels today's realized P&L as "yesterday" (cosmetic, value is causal)
- **dimension/file:** backtest-correctness · `engine.py` engine.py:102-105, 125 (prev_ret threading) and prompts.py:41-46 (build_reflection)
- **claim:** At decision day T = days[i], the reflection is built from prev_ret, which is port_r from iteration i-1. Iteration i-1 had nxt = days[i], so prev_ret is the portfolio return REALIZED ON day T itself (close[T]/close[T-1]-1 weighted by w(T-1)), not on T-1. build_reflection labels this 'gained/lost X% yesterday'. The numeric value uses only data <= T (close[T] is in the agent's allowed information set at the close of T), so it is NOT future leakage. The only defect is the wording 'yesterday', which actually describes today's already-realized move.
- **fix:** Either relabel the reflection text to 'today' / 'most recent session' to match the value, or (if 'yesterday' is intended) pass the previous-previous iteration's return. No change is required for correctness of results; this is wording-only. Confidence is high that it is causal, hence low severity.

### 14. [LOW] Causality assertion is a tautology given .loc[:day] semantics (provides no real guard)
- **dimension/file:** pipeline-causality · `context.py` lines 43-50 (hist = close.loc[:day].tail(...); assert hist.index.max() <= day)
- **claim:** hist is built as close.loc[:day].tail(trailing_days). On a sorted DatetimeIndex, label slicing .loc[:day] can NEVER return a row dated after day by construction, so the subsequent assert hist.index.max() <= day is structurally always true and can never fire. I verified empirically that on both an exact trading day (2024-09-11) and a non-trading day (Sat 2024-09-14, which correctly stops at Fri 9/13) the max index is always <= day. The assertion is therefore a no-op as a leakage detector.
- **fix:** Make the guard test something that can actually fail: assert against the realization day, not the decision day. E.g. assert the context max date is strictly less than the next trading day used to realize returns, or assert hist.index.max() == day for trading days / the expected prior trading day for non-trading days, AND assert hist.index.is_monotonic_increasing and not hist.index.has_duplicates. 

### 15. [LOW] rationale_forensics counts denials/negations and macro-neutral mentions as HARD smoking-gun tells (no negation handling)
- **dimension/file:** metric-validity · `leakage.py` rationale_forensics, lines 118-154 (substring match `kw in text` at line 130; aggregation into hard_hits / n_hard_decisions lines 138-142)
- **claim:** Detection is pure case-insensitive substring matching: `hard_found = [kw for kw in HARD_TELLS if kw in text]`. There is no negation/denial awareness. A model that writes 'I have no information about the August 5 crash', 'I am not assuming any election outcome', or 'cannot predict a rate cut' will match 'august 5', 'election', 'rate cut' and be counted as a HARD tell, incrementing hard_tell_counts and n_hard_decisions. Several HARD tokens ('election','rate cut','rate hike','tariff','recession') are also generic macro vocabulary a pre-cutoff control model (C-A) could legitimately produce without
- **fix:** Make matching negation-aware: drop a hit if the keyword is immediately preceded by a denial cue ('no ', 'not ', 'without ', 'cannot ', "don't", 'unaware of', 'no information', 'no knowledge') within a small window, or require the tell to co-occur with a directional/forward claim. At minimum, report hits with their surrounding sentence and have a human adjudicate before counting; and split HARD_TEL

### 16. [LOW] pre_event_timing baseline (mean_expo) is computed over the full window including event and post-event days, contaminating the reference exposure
- **dimension/file:** metric-validity · `leakage.py` pre_event_timing, line 81 `mean_expo = float(expo.mean())` used against `expo_pre` at lines 88-90
- **claim:** `mean_expo` averages exposure across ALL decision days in the run, including the event day and every day after it. For a down event where the agent de-risks at the crash and stays de-risked, those low post-event exposures pull `mean_expo` down, shrinking `mean_expo - expo_pre`. For the up (election) event, loading up and staying loaded pulls `mean_expo` up, shrinking `expo_pre - mean_expo`.
- **fix:** Define the baseline from PRE-event days only, e.g. `mean_expo_pre = expo[expo.index < d].mean()` (or a trailing k_baseline window before d, which the unused `k_baseline` parameter already hints at), so the reference is the agent's normal exposure leading into the event rather than its whole-window average. Note `k_baseline` is currently accepted but never used.

### 17. [LOW] MockClient null-reference is not reproducible across processes (unseeded Python hash) and breaks resume correctness for the offline pipeline validation
- **dimension/file:** agent-parsing · `llm_client.py` llm_client.py:94 (rng = np.random.default_rng(seed + abs(hash(user)) % (2**31)))
- **claim:** MockClient is documented as a 'deterministic, seeded' null reference whose expected leakage is ~0 and which is used to validate the metrics pipeline (module docstring lines 4-7, 80-86; smoke.py uses mock=True). Its RNG seed mixes in abs(hash(user)), but Python's built-in str hash is salted per-process via PYTHONHASHSEED, which is not pinned anywhere in the repo (verified: hash('abc') differs every interpreter invocation; no PYTHONHASHSEED set). Therefore the same (seed, prompt) yields different weights on different process runs.
- **fix:** Replace abs(hash(user)) with a stable hash of the prompt, e.g. int.from_bytes(hashlib.sha256(user.encode()).digest()[:8], 'big') % (2**31), so the mock is deterministic regardless of PYTHONHASHSEED and resume-safe. (Alternatively set PYTHONHASHSEED in the run entrypoint, but a stable hash is more robust.)

### 18. [LOW] EXIT-trap self-terminate and HOME fix are correct and well-placed (positive verification)
- **dimension/file:** infra-repro · `bootstrap.sh` lines 6, 10, 21-34, 75-78
- **claim:** Verified: (1) export HOME="${HOME:-/root}" at line 10 resolves to /root for BOTH unset and empty HOME (tested) and sits before the first HOME-dependent command (ollama install at line 48). (2) The EXIT trap (line 34) is installed before the first failure-prone command (apt-get at line 47); only var assignments, the tee-exec, echoes, and function defs precede it. (3) set -e aborts (including a failing command substitution) fire the EXIT trap, and self_terminate falls back to shutdown -h now (with instance-initiated-shutdown-behavior=terminate) if awscli/metadata is unavailable. (4) The decision
- **fix:** No change required for the trap/HOME/cache wiring. (Operational note, not a code bug: results/EC2_INSTANCE.txt records a launched g6e.xlarge spot in ap-northeast-2a via overrides while launch_spot.sh defaults to g5.xlarge/us-east-1 — confirm that instance actually self-terminated, since the higher-cost g6e increases the blast radius if any silent-failure path above leaves it spinning on retries.)

## Adversarially refuted (NOT issues)

- (design-confounds) Price-only context does not isolate parametric memory; trailing-price momentum can predict next-day moves, so 'foresight' may be plain momentum
  - why refuted: REFUTED as a validity-affecting confound. The claim's physics is correct in isolation (trailing returns/vol in context.py:54-62 are legitimate price-only signals; if next-day returns are autocorrelated, a pure momentum/vol-target rider would produce nonzero ticker_prescience/exposure_timing/pre_even
- (design-confounds) Treatment-model selection is circular: pick the model that best recalls 2024-H2, then test whether it exploits 2024-H2
  - why refuted: REFUTED. The claim mislabels a treatment manipulation-check as "selection on the dependent variable," and its proposed bias mechanism does not exist in this design.

1) Selection variable ≠ dependent variable. In model_selection.py, pick_best (lines 90-95) maximizes res[m]["score_2024H2"], which pro
- (design-confounds) C-A control differs from treatment in family AND size (8B llama vs up to 32B qwen), an uncontrolled capability confound the design acknowledges but the run amplifies
  - why refuted: The claim's FACTUAL premises check out, but its IMPACT (that this invalidates or seriously undermines the leakage conclusion) is refuted by the actual design.

Confirmed facts:
1. EC2 run does widen the C-A gap. infra/bootstrap.sh:53-72 pulls llama3.1:8b as control and auto-selects the treatment fro
- (design-confounds) rationale_forensics future-month detector is biased toward false positives late in each window and ignores year, undercutting the 'smoking-gun' channel
  - why refuted: I read src/leakage/metrics/leakage.py (rationale_forensics lines 118-154, future-month logic 132-137), plus orchestrate.py, report_tables.py, and config.py to trace how the outputs are used.

Mechanical facts in the claim are partly correct: the future-month scan parses only the month component of d
- (infra-repro) Empty/garbage TREAT from selection is not validated before export; an empty or stray value would be exported as the treatment model
  - why refuted: REFUTED. The claim is literally true that lines 70-73 of /Users/jeon-younghoon/Desktop/lectures/finance_ai/homework/infra/bootstrap.sh capture TREAT via `... python3 -m leakage.run.model_selection --pick qwen3:32b qwen2.5:32b 2>>\"$LOG\" | tail -1` and export it without a `case` validation. But the 